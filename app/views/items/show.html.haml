%h1 Item

= link_to 'Edit', edit_store_item_path(@item.store, @item)
\|
= link_to 'Back', store_items_path(@item.store)
|
= link_to 'Add photo', new_store_item_photo_path(@item.store, @item)

%p
  %b Title:
  = @item.title
%p
  %b Body:
  = @item.description

%p
  %i Note: Photos are sortable. Just drag and drop into new position

.container
  .photos#sortable
    %h2 Photos

    - @item.photos.forward.each do |photo|
      = render photo

  .queue
    / This division is replaced with a flash button by Uploadify
    #photo_upload

/ Only load uploadify javascript on pages they are needed
= javascript_include_tag '/uploadify/swfobject.js', '/uploadify/jquery.uploadify.v2.1.0.min.js'

/ See application_helper.rb for this code
= photo_uploadify

/ And more Javascript
= javascript_include_tag 'jquery-ui.min'

/ application_helper
= photo_sortable
